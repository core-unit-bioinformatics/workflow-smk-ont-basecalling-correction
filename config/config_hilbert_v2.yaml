# Global configuration for the workflow "workflow-smk-ont-basecalling-correction"

# Path to the samplesheet (must be TSV with columns: sample_name, file_path)
samplesheet: "/gpfs/project/projects/medbioinf/users/semey101/ont-basecalling-correction/samples/samples_complete_hilbert_bc01_v2.tsv"        

# Paths to the Dorado installation and the needed models (needs to be modified to fit the users installation paths)
dorado_bin: "dorado"     # TO DO: add code for an online/offline switch.
dorado_sup_model: "/gpfs/project/projects/medbioinf/users/semey101/ont-basecalling-correction/dorado-0.9.6-linux-x64/models/dna_r10.4.1_e8.2_400bps_sup@v4.1.0"  # TO DO: add code for an online/offline switch.
dorado_correction_model: "/gpfs/project/projects/medbioinf/users/semey101/ont-basecalling-correction/dorado-0.9.6-linux-x64/models/herro-v1"   # TO DO: add code for an online/offline switch.

# Dorado kit (needs to be modified to fit the users parameters)
dorado_kit: "SQK-NBD114-24"

# Paths to the pod5 installation (is installed via conda in this path if online; otherwise needs to be modified to fit the users installation paths or be included in PATH)
pod5_convert_bin: "pod5"    # TO DO: add code for an online/offline switch.

# Resources                 # TO DO: allocate these within the modules?
cpu_correct: 16
cpu_basecall: 16
cpu_convert: 4

mem_correct: 96000    # 32gb 
mem_basecall: 96000    # 32gb 
mem_convert: 4000      # 4gb

gpu_correct: 4
gpu_basecall: 4